# AI-image-detector
AI Image Authenticity Detector is a deep learning-based web project that predicts whether an uploaded image is real or fake. It uses a Flask interface with a trained PyTorch model to analyze images and display results along with confidence scores in a simple and user-friendly way.
